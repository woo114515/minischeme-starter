# mini-Scheme 解释器挑战

以下由AI生成。

## 1. 任务目标与考察内容

**任务**：vibe coding，从零写出一个符合 `spec.md` 的 mini-Scheme 解释器，让全部验收测试通过。预计 30 分钟。你不需要会编程——你负责向 AI 描述需求、验证结果、报错时继续迭代。

代码放在 `src/` 目录（需要你自己创建），入口文件必须是 `src/main.py`（评分器按这个路径调用，运行方式见 spec §2）。

考察三项：

1. **功能正确**：解释器能否通过全部验收测试（底线）。
2. **模块化**：代码按流水线拆成职责单一的小模块。
3. **代码规范**：例如合适的注释，是否有足够的鲁棒性。

## 2. Scheme 语言简介与模块化思路

### Scheme 是什么

mini-Scheme 是一种极简的编程语言：几乎一切都写成 `(操作符 参数...)` 的形式——操作符写在最前面，括号决定运算顺序。例如 `(+ 1 2)` 表示 1 加 2。完整的语法规则见 `spec.md`。

### 一个解释器怎么运转

解释器就是"读懂一段程序文本，算出结果，再打印出来"的程序。流水线只有三步：

```
程序文本 ──读──→ 表达式 ──算──→ 值 ──打印──→ 结果
```

**模块化**就是把这条流水线拆成职责单一的小模块：读的归读的、算的归算的、打印的归打印的。这样每一步都能独立验证——测试挂了，你能立刻知道问题出在哪一步，也知道该让 AI 改哪个文件。怎么拆由你（和 AI）决定，建议见 §4。

## 3. Autograder 使用方法

评分器 `autograder.pyz` 已经放在本仓库根目录。在仓库根目录运行：

```
python3 autograder.pyz python3 src/main.py
```

- 它会运行 12 组验收用例，每组是一段 mini-Scheme 程序，逐字节比对输出。
- `PASS` 表示通过；`FAIL` 会显示你的解释器的报错信息，或"输出不符"加上你的实际输出——评分器不会把期望输出打印给你，请对照 spec 判断哪里不对。
- 只跑一组：`python3 autograder.pyz python3 src/main.py --case=006`。
- 用例难度递增。**别一次冲全部**：先让第一组通过，再逐步推进。
- 自测器只是自查工具，正式评分另有你从未见过的用例；通过自测 ≠ 满分——把 spec 的每个特性都用自己写的程序验证过才算数。
- `example/` 里有六个从易到难的示例程序，每行注释标注期望输出，可逐个核对：`python3 src/main.py example/01_arithmetic.scm`。

## 4. 完成提示

### 建议的模块划分

参考流水线，常见拆法是：

| 模块 | 职责 |
|---|---|
| 词法 | 程序文本 → 词（token）列表 |
| 语法 | 词 → 表达式（嵌套的数据结构） |
| 环境 | 变量绑定表 + 指向外层环境的指针（词法作用域的关键） |
| 打印 | 值 → 文本 |
| 内置库 | spec §5 的全部内置函数，放进初始环境 |
| 求值器 | 表达式 → 值（心脏） |
| 入口 | 读文件/标准输入、逐行打印结果，即 `src/main.py` |

### 求值器的核心：evaluate 与 apply 互相调用

整个解释器的心脏是两个**互相递归**的函数：

- `evaluate(expr, env)`：求值一个表达式。符号 → 去环境查值；数字/布尔/字符串 → 原样返回；括号表达式 → 特殊形式（if/cond/define/lambda/let/quote/and/or/begin）按 spec §4 各自的求值顺序处理，其余是函数调用——先求值操作符和全部实参，再交给 apply。
- `apply(proc, args)`：调用一个过程。内置过程 → 直接调用；用户函数（闭包）→ 新建一层环境把实参绑到参数名上（外层指向函数**定义时**的环境），再回到 evaluate 求值函数体。

evaluate 遇到函数调用就调 apply，apply 执行函数体又调回 evaluate——想通这个循环，解释器就成了一半。

