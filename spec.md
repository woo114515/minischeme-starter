# mini-Scheme 语言规范

> 本文档面向**完全没有编程经验**的学生，不需要任何编程基础。它是 mini-Scheme（一个从 Scheme 语言裁剪出的小子集，参考 CS61A 的 Scheme 核心）的完整语言手册。遇到看不懂的词，把它交给 AI 解释即可。

实现语言规定为 **Python 3**（为简便统一）：学生从本仓库的 `src/` 骨架起步，把它补全为一个能通过验收测试的解释器（骨架用法见 `README.md`）。

## 1. 最重要的语法规则：语序（前缀表达式）

mini-Scheme 里几乎一切都是同一个形状：**一对圆括号，操作符写在最前面**。这叫"前缀表达式"。这和数学课上的写法正好相反。

数学写法和 mini-Scheme 写法的对照：

| 数学写法 | mini-Scheme 写法 |
|---|---|
| `1 + 2` | `(+ 1 2)` |
| `3 × 4` | `(* 3 4)` |
| `10 − 4 − 1` | `(- 10 4 1)` |
| `(1 + 2) × 3` | `(* (+ 1 2) 3)` |
| `7 ÷ 2 取整数商` | `(/ 7 2)` |

三条铁律：

1. **一切表达式都要用一对圆括号包起来**（数字、布尔、字符串这些"基本值"除外）。
2. **操作符/函数名永远写在最前面**——先写"要做什么"，再写"对谁做"。
3. **括号决定运算顺序**：永远先算最内层括号里的内容，由内向外。

看一个逐步求值的过程（`;` 后面是注释，不算程序）：

```
(* (+ 1 2) 3)
→ (* 3 3)        ; 先算最内层的 (+ 1 2)
→ 9              ; 再算 (* 3 3)
```

最常见的两种语序错误：

- 写成数学的中缀 `(1 + 2)`：解释器会把 `1` 当成"函数"去调用，报错。正确写法是 `(+ 1 2)`。
- 漏掉外层括号：`* 3 4` 不是合法表达式，`(* 3 4)` 才是。

不只是算术，**所有操作都遵循前缀语序**：`(car '(1 2 3))` 表示"对 `(1 2 3)` 做 `car`"，`(if (> x 0) '正 '负)` 表示"先判断 `x` 是否大于 0，再选一个结果"。

## 2. 运行方式（CLI 约定）

解释器从命令行接受一个或多个 `.scm` 文件；没有参数时从标准输入读取：

```
python3 your-interpreter.py file1.scm [file2.scm ...]
```

- 按顺序求值文件中每一个顶层表达式，**每个求值结果独占一行打印**。
- 求值结果为 `None`（无值）时不打印（如 `display`、`newline` 的结果）。
- 多个文件共享同一个全局环境（后一文件可见前一文件的 `define`）。
- 每个测试用例在独立进程中运行，初始环境为空。

## 3. 词法：程序由什么组成

程序就是一段文本，由这些"零件"拼成：

| 零件 | 例子 | 说明 |
|---|---|---|
| 注释 | `; 从这里到行尾都被忽略` | `;` 开始到行尾 |
| 整数 | `42`、`-7`、`0` | 浮点数可选，验收测试只用整数 |
| 布尔 | `#t`（真）、`#f`（假） | 只有这两种写法 |
| 符号 | `foo`、`x1`、`+` | 表示"名字"，不是变量就是操作符 |
| 字符串 | `"hello world"` | 双引号包住；`\n` `\t` `\"` `\\` 是转义 |
| 引用简写 | `'x`、`'(1 2 3)` | `'x` 是 `(quote x)` 的简写，见 §4 |
| 列表语法 | `(1 2 3)` | 括号里的多个值，见 §6 |

- 零件之间用空白（空格、换行、制表符）分开。
- 大小写敏感：`foo` 和 `FOO` 是两个不同的符号。

## 4. 特殊形式（语法规则）

特殊形式长得像函数调用，但**求值规则不同**——它们自己决定"先算什么、后算什么"。每个形式下方都注明了求值顺序，这是最容易出错的地方。

### 4.1 quote —— 原样返回，不求值

```
(quote 数据)     '数据 是它的简写
```

`quote` 把数据"原封不动"地交出来。不加 quote 的话，`(1 2 3)` 会被当成"调用函数 1"而报错；加了 `'(1 2 3)`，它就是一个数据列表。

```
'x           → x           ; 符号本身
'(1 2 3)     → (1 2 3)     ; 一个列表
'()          → ()          ; 空表
```

### 4.2 if —— 只走一个分支

```
(if 测试 真分支 假分支?)
```

求值顺序：**先求值"测试"**；结果不是 `#f` 就求值"真分支"，否则求值"假分支"。另一个分支**根本不会被执行**。假分支可以省略，此时测试为 `#f` 结果就是 `None`（不打印）。

```
(if (> 3 2) 'yes 'no)   → yes
(if #f 'bad 'good)      → good
(if (null? '()) 1 2)    → 1
```

### 4.3 cond —— 多分支选择

```
(cond (测试1 表达式...) (测试2 表达式...) ... (else 表达式...))
```

求值顺序：从上到下依次求值每个"测试"，遇到第一个不为 `#f` 的，就按 `begin` 语义依次求值它后面的表达式并返回最后一个。子句里没有表达式时，返回测试值本身。`else` 子句作为兜底，通常放最后。全部不匹配返回 `None`。

```
(cond ((= 1 2) 'a) ((= 2 2) 'b) (else 'c))   → b
(cond (else 'd))                             → d
(cond ((> 2 3)) (else 42))                   → 42
```

### 4.4 and / or —— 短路求值

```
(and e1 e2 ...)   (or e1 e2 ...)
```

求值顺序：从左到右，**一旦能确定结果就立刻停下，后面的表达式不执行**（这叫短路）。

- `and`：遇到第一个 `#f` 立刻返回 `#f`；全部为真则返回最后一个的值；`(and)` → `#t`。
- `or`：遇到第一个不为 `#f` 的值立刻返回它；全为 `#f` 则返回 `#f`；`(or)` → `#f`。

```
(and #f (/ 1 0))   → #f        ; (/ 1 0) 根本没执行，不会报错
(or #t (/ 1 0))    → #t        ; 同上
```

注意：只有 `#f` 是假。`0`、`()` 都是真。

### 4.5 define —— 定义名字

```
(define 名 表达式)
(define (函数名 参数...) 体...)
```

求值顺序：**先求值右侧的表达式**，再在当前环境绑定。`define` 的结果是被定义的符号名（顶层会打印出来）。

第二种写法是"定义函数"的简写，等价于 `(define 函数名 (lambda (参数...) 体...))`。

```
(define pi 3)                          ; 结果打印 pi
(+ pi 1)                    → 4
(define (square x) (* x x))            ; 函数简写
(square 7)                  → 49
```

函数体内可以调用自己的名字（递归），见 §7。

### 4.6 lambda —— 制造函数

```
(lambda (参数...) 体...)
```

求值顺序：**函数体此时不求值**，等到以后被调用才求值。`lambda` 产生一个"闭包"——它会记住定义时所在的环境（参数个数固定）。

```
(define square (lambda (x) (* x x)))
((lambda (x) (* x 2)) 5)   → 10
```

### 4.7 let —— 局部变量

```
(let ((名 表达式)...) 体...)
```

求值顺序：**先把每个绑定表达式在外层环境求值完**（并行绑定——绑定之间互不可见），再在新环境里按 `begin` 语义求值函数体。

```
(let ((x 3) (y 4)) (+ x y))           → 7
(let ((a 1)) (let ((b (+ a 1))) (* a b)))   → 2
```

### 4.8 begin —— 顺序执行

```
(begin e1 e2 ...)
```

求值顺序：从左到右依次求值，返回最后一个的结果。

```
(begin (define z 1) (+ z 41))   → 42
```

## 5. 内置过程（标准函数库）

内置过程就是解释器自带的函数，**参数先全部求值，再调用**（和特殊形式不同）。

| 分类 | 过程 | 说明与例子 |
|---|---|---|
| 算术 | `+` | 可变参数求和：`(+ 1 2 3)` → `6` |
| 算术 | `-` | 可变参数；单参数取反：`(- 10 4 1)` → `5`，`(- 5)` → `-5` |
| 算术 | `*` | 可变参数求积：`(* 2 3 4)` → `24` |
| 算术 | `/` | 可变参数；**整数相除得整数商**：`(/ 7 2)` → `3`；单参数求倒数 |
| 算术 | `modulo` | 整数取模：`(modulo 17 5)` → `2` |
| 算术 | `quotient` | 整除商：`(quotient 17 5)` → `3` |
| 算术 | `expt` | 乘方：`(expt 2 10)` → `1024` |
| 算术 | `abs` | 绝对值：`(abs (- 5 9))` → `4` |
| 比较 | `= < > <= >=` | 链式比较（相邻两两都成立才为真），支持数字与符号：`(< 2 3 4)` → `#t` |
| 布尔 | `not` | 取反：`(not #f)` → `#t`；`(not 0)` → `#f`（因为只有 `#f` 是假） |
| 列表 | `cons` | 构造点对：`(cons 1 2)` → `(1 . 2)` |
| 列表 | `car` | 取点对的第一项：`(car '(1 2 3))` → `1` |
| 列表 | `cdr` | 取点对的第二项：`(cdr '(1 2 3))` → `(2 3)` |
| 列表 | `list` | 打包成列表：`(list 1 2 3)` → `(1 2 3)` |
| 列表 | `length` | 列表长度：`(length '(a b c d))` → `4` |
| 列表 | `append` | 拼接若干列表：`(append '(1 2) '(3 4))` → `(1 2 3 4)` |
| 列表 | `null?` | 是否空表：`(null? '())` → `#t` |
| 列表 | `pair?` | 是否点对（非空）：`(pair? '(1 2))` → `#t`，`(pair? '())` → `#f` |
| 列表 | `list?` | 是否真列表：`(list? '(1 2))` → `#t`，`(list? (cons 1 2))` → `#f` |
| 谓词 | `number?` | `(number? 5)` → `#t`；`(number? 'x)` → `#f` |
| 谓词 | `boolean?` | `(boolean? #t)` → `#t` |
| 谓词 | `symbol?` | `(symbol? 'x)` → `#t` |
| 谓词 | `string?` | `(string? "s")` → `#t` |
| 谓词 | `procedure?` | `(procedure? car)` → `#t`；`(procedure? 1)` → `#f` |
| 谓词 | `zero? even? odd?` | 数值谓词（参数必须是数）：`(even? 8)` → `#t` |
| 谓词 | `eq?` | 符号、数字、布尔按值比较；列表等复合数据按同一性（是否同一对象）：`(eq? '() '())` → `#t`，`(eq? '(1) '(1))` → `#f` |
| 谓词 | `equal?` | 结构相等（逐层比较）：`(equal? '(1 2 3) (list 1 2 3))` → `#t` |
| 输出 | `display` | 打印一个值（字符串不带引号），不换行 |
| 输出 | `newline` | 输出一个换行 |

## 6. 列表：用点对链表示数据

mini-Scheme 里列表不是"装很多格子的盒子"，而是**一串点对**。一个点对是两项的组合；列表 `(1 2 3)` 实际上是：

```
(1 . (2 . (3 . ())))      ; 每一步的第一项是元素，第二项指向下一段
```

`()`（空表）表示链的终点。`(1 2 3)` 这种写法只是这条链的简写。

- `(car '(1 2 3))` → `1`：第一项
- `(cdr '(1 2 3))` → `(2 3)`：剩下的链
- `(cons 1 '(2 3))` → `(1 2 3)`：把 1 接到链头
- `(cons 1 2)` → `(1 . 2)`：第二项不是链，这就是一个"点对"（打印带点）

`'(1 2 3)` 和 `(list 1 2 3)` 结果相同，但前者是数据写法（不求值），后者是函数调用（每一项都先求值）。

## 7. 递归入门（零基础必读）

递归是"函数调用自己"。零基础也能看懂，只需要抓住三点：

1. **终止条件**：什么情况下直接给答案、不再调用自己。
2. **缩小规模**：每次调用自己时，问题比上次小一点。
3. **组合结果**：用自己的答案拼出当前问题的答案。

阶乘就是标准例子：`n! = n × (n-1)!`，而 `0! = 1`。

```
(define (fact n) (if (= n 0) 1 (* n (fact (- n 1)))))
```

求值 `(fact 3)` 的展开过程：

```
(fact 3)
→ (* 3 (fact 2))
→ (* 3 (* 2 (fact 1)))
→ (* 3 (* 2 (* 1 (fact 0))))
→ (* 3 (* 2 (* 1 1)))        ; (= 0 0) 为真，走终止条件
→ 6
```

`define` 定义的函数在函数体内可以使用自己的名字，所以递归不需要任何额外语法。在列表上递归同样常见：对空表返回 `'()`，否则处理 `(car xs)` 再递归 `(cdr xs)`——验收测试 009 里的 `map`/`filter` 就是这么定义的。

## 8. 值的打印

| 值 | 打印 |
|---|---|
| 整数 | `42` |
| 布尔 | `#t` / `#f` |
| 符号 | `foo` |
| 字符串 | `"hello"`（带引号；`display` 输出时不带） |
| 真列表 | `(1 2 3)` |
| 点对 | `(1 . 2)` |
| 空表 | `()` |
| 过程 | `#<procedure>`（验收测试不依赖其确切格式） |

## 9. 求值规则小结

- **应用序求值**：函数调用先求值操作符和全部实参，再调用；特殊形式按各自的规则（§4）。
- **词法作用域 + 闭包**：`lambda` 捕获定义处环境；`define` 的函数体内可见自己的名字。
- 只有 `#f` 为假：`0`、`()`、`""` 均为真。

## 10. 不在范围内

`set!`、`set-car!`/`set-cdr!`、quasiquote、向量、字符、字符串函数、可变参数 lambda、宏、尾调用优化保证、错误处理策略（出错时行为未定义，测试不触发错误）。`map`/`filter`/`apply` **不内置**——它们是验收测试中要求用语言本身定义的函数。

## 11. 排错指南（测试不通过时看这里）

| 现象 | 原因 | 改法 |
|---|---|---|
| 报错说"1 不是过程/无法调用" | 写成了中缀 `(1 + 2)` | 语序：`(+ 1 2)` |
| `(car (1 2 3))` 报错 | 数据没加 quote，被当成函数调用 | `(car '(1 2 3))` |
| 输出比预期多一行 | `define` 顶层会打印符号名 | 属正常现象，逐行对 `.out` |
| 输出比预期少一行 | `display`/`newline` 的结果是 `None`，不打印 | 属正常现象 |
| `(/ 7 2)` 得到 `3.5` | 整数相除要得整数商 | 按 §5 实现 |
| `(and #f (/ 1 0))` 报错 | `and`/`or` 没做短路 | 必须实现短路 |
| 闭包用例（008/009）失败 | `lambda` 没记住定义时的环境 | 闭包要保存环境 |
| `let` 用例（010）失败 | 绑定串行求值 | let 是**并行**绑定 |
| `equal?` 把不同列表判成相等 | 用了 Python 的 `==` 比 tuple | 要递归比较结构 |
| 列表打印成 `(1 2 . 3)` 或卡死 | 引用数据没转成点对链 | 见 starter/parser.py 的 `to_chain` |

## 12. 验收测试

- 验收用例共 12 组，难度递增，打包在评分器 `autograder.pyz` 中（自测用，只报告通过/失败与你的输出，不给期望输出）。
- 测试你的解释器：`python3 autograder.pyz python3 src/main.py`（单组：加 `--case=NNN`）。
- 参考实现、完整用例与开发用运行器仅教师持有，不随本仓库发布。
- 学生从本仓库的 `src/` 骨架起步，使用说明见 `README.md`。

用例阶梯：001–003 基本求值（算术/比较/短路布尔）→ 004–005 控制流与函数 → 006 递归 → 007 列表 → 008 闭包 → 009 用语言本身定义 `map`/`filter` → 010 `let`/`begin` → 011 输出 → 012 谓词。

评分：12 组用例全部通过之外，还考察模块化——骨架文件的职责划分是否清晰、每个模块能否独立验证（见 `starter/README.md` 的评分关注）。
